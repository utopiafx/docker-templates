version:'3'
services:
	db:
		image: postgres:10.5-alpine
		restart: always
		volumes:
			#- db_data:/var/lib/postgresql/data
			- ./dbdata:/var/lib/postgresql/data
		expose:
			- "5432"
		env_file:
			db.env
# This is being use to store permament
#volumes:
#	db_data: